import React from 'react';

/**
 * Filters for expense list (date range, category, payment method, tags)
 */
// PUBLIC_INTERFACE
export default function ExpenseFilters() {
  /** Placeholder filters. */
  return (
    <div>
      <h3>Filters</h3>
      <p>Controls for date range, category, payment method, tags.</p>
    </div>
  );
}
